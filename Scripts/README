Instructions

1. Get a .env file
2. Run 'docker-compose up' or 'docker-compose up -d'
3. Set your working directory to the scripts folder.
4. Run 'docker exec -i flightsapi-database psql -U flightsapi -d flightsapi < seed.sql'

You should now have bogus data for the API endpoints so that you can build some cool GUIs.
